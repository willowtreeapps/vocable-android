package com.willowtree.vocable.ui.resetsettings

/** The individually-resettable settings domains offered on the Reset App Settings screen. */
enum class ResetDomain {
    VOICE,
    SENSITIVITY,
    SELECTION_MODE,
    CATEGORIES,
    PHRASES
}
